<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Sign Up</title>
  <!-- Hinglish: Tailwind Play CDN (learning ke liye best, prod me proper setup karna) -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gray-100 flex items-center justify-center">
  <div class="w-full max-w-md bg-white shadow-lg rounded-xl p-6">
    <h1 class="text-2xl font-bold text-center mb-4">Create Account</h1>

    <% if (error) { %>
      <div class="mb-4 p-3 rounded bg-red-100 text-red-700 text-sm"><%= error %></div>
    <% } %>

    <form action="/signup" method="POST" class="space-y-4">
      <!-- Username -->
      <div>
        <label class="block text-sm mb-1">Username</label>
        <input
          type="text"
          name="username"
          value="<%= old.username %>"
          class="w-full border rounded-md p-2 outline-none focus:ring"
          placeholder="Apna naam likho"
          required
        />
      </div>

      <!-- Email -->
      <div>
        <label class="block text-sm mb-1">Email</label>
        <input
          type="email"
          name="email"
          value="<%= old.email %>"
          class="w-full border rounded-md p-2 outline-none focus:ring"
          placeholder="example@mail.com"
          required
        />
      </div>

      <!-- Password -->
      <div>
        <label class="block text-sm mb-1">Password</label>
        <input
          type="password"
          name="password"
          class="w-full border rounded-md p-2 outline-none focus:ring"
          placeholder="Strong password daalo"
          required
        />
      </div>

      <button
        type="submit"
        class="w-full py-2 rounded-md font-semibold bg-black text-white hover:opacity-90"
      >
        Sign Up
      </button>
    </form>

    <p class="text-xs text-gray-500 mt-4">
      Tip: Password hum hash karke store kar rahe (plain text nahi).
    </p>
  </div>
</body>
</html>
